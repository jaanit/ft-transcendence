import * as THREE from 'three'
import React, { forwardRef, useLayoutEffect, useRef, useMemo } from 'react'
import { useLoader } from '@react-three/fiber'

export function Text({number}:{number:number})
{
  
}